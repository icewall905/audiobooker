# Core dependencies for Chatterbox TTS audiobook creator
chatterbox-tts
torch>=1.13.0
torchaudio>=0.13.0

# Web interface dependencies
gradio>=4.0.0

# Optional: MP3 conversion (requires ffmpeg system package)
# ffmpeg-python>=0.2.0
